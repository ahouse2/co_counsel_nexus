# Build Log — 2025-11-09

- **Scope:** Bug remediation for ACE tooling dependency regressions uncovered by cross-suite review.
- **Highlights:**
  - Identified missing `jsonschema` dependency causing `pytest tools/tests` collection failure via ModuleNotFoundError.
  - Pinned `jsonschema==4.23.0` in shared dependency manifest to stabilise ACE schema validators.
  - Revalidated both ACE tooling and backend suites post-installation to confirm clean regression baseline.
- **Tests:**
  - `pip install -r backend/requirements.txt`
  - `pytest tools/tests -q`
  - `pytest backend/tests -q`
- **Notes:**
  - Captured DeprecationWarning for `jsonschema.RefResolver`; future enhancement could migrate to `referencing` resolver API.
  - Stewardship artefacts updated (ACE memory append, Chain of Stewardship log) to reflect dependency governance outcome.
